{{- if and .Values.hermesConfig.seed.enabled (not .Values.persistentStorage.hermesHome.enabled) -}}
{{- fail "hermesConfig.seed.enabled requires persistentStorage.hermesHome.enabled=true so the generated config.yaml can be persisted" -}}
{{- end -}}
{{- if and .Values.hermesConfig.seed.enabled (not .Values.hermesConfig.seed.existingSecret) (not .Values.hermesConfig.seed.config) (not .Values.hermesConfig.seed.envConfig) -}}
{{- fail "hermesConfig.seed.enabled requires hermesConfig.seed.config, hermesConfig.seed.envConfig, or hermesConfig.seed.existingSecret" -}}
{{- end -}}
