{{- if and .Values.hermesConfig.seed.enabled (not .Values.persistentStorage.hermesHome.enabled) -}}
{{- fail "hermesConfig.seed.enabled requires persistentStorage.hermesHome.enabled=true so the generated config.yaml can be persisted" -}}
{{- end -}}
{{- if and .Values.hermesConfig.seed.enabled (not .Values.hermesConfig.seed.existingSecret) (not .Values.hermesConfig.seed.config) -}}
{{- fail "hermesConfig.seed.enabled requires either hermesConfig.seed.config or hermesConfig.seed.existingSecret" -}}
{{- end -}}
